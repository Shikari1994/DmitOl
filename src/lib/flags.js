/* ─────────────────────────────────────────────
   Флаги фич.

   Здесь только выключатели: ни логики, ни стилей.
   ───────────────────────────────────────────── */

/* Светлая схема. Выключена 2026-07-30 перед выкладкой на хостинг —
   решено пока показывать сайт только в тёмном виде, к светлому,
   возможно, вернёмся.

   ВЫКЛЮЧЕНА ТОЛЬКО ТОЧКА ВХОДА. Сам светлый слой цел и никуда не
   делся: правила html[data-scheme="light"] лежат где лежали —
   foundation/scheme-light.css (базовая палитра) плюс оверрайды в
   sections/{software,atlas,equipment,hero,nav,product-page,economics}.css.
   Мёртвый селектор, который не сматчится ни разу, стоит в рантайме
   ноль, поэтому его не тронули: закомментированные вразнос 65 блоков
   в семи файлах сгнили бы молча, а тут любая правка секции идёт
   мимо них так же, как и раньше.

   Флаг гасит четыре места:
   - Layout.astro — html[data-scheme] прибит к "dark" прямо в разметке
     (при включённом флаге его синхронно ставит inline-скрипт по
     localStorage + prefers-color-scheme);
   - Nav.astro — обе копии кнопки .theme-toggle (шапка и мобильное
     меню) и светлый вариант лого .nav-logo-img--light;
   - scripts/nav.js — обработчик клика по тумблеру;
   - Software.astro — светлый ролик «экосистемы» (.sw-eco-video--light).

   ЧТОБЫ ВЕРНУТЬ: поставить true и достать из архива
   media-archive/on-hold/ecosystem-light.mp4 назад в public/video/
   (2.3 МБ, нужен только светлой схеме — без него светлая плитка
   «экосистемы» останется пустой). Заведомо рабочее состояние темы —
   git-тег light-theme-v1.

   ЧЕГО ЖДАТЬ ПРИ ВОЗВРАТЕ: светлые оверрайды есть у секций, которые
   существовали на 2026-07-30. Всё, что появится в разделах позже,
   светлого варианта не получит — его никто не увидит в браузере, пока
   флаг false, поэтому проверять придётся заново по всем секциям. */
export const ENABLE_LIGHT_THEME = false
